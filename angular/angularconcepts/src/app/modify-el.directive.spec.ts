import { ModifyElDirective } from './modify-el.directive';

describe('ModifyElDirective', () => {
  it('should create an instance', () => {
    const directive = new ModifyElDirective();
    expect(directive).toBeTruthy();
  });
});
